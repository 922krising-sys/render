{
  "name": "rupeerates-proxy",
  "version": "1.0.0",
  "description": "Rate proxy server for RupeeRates — fetches live rates from UAE exchange houses",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
